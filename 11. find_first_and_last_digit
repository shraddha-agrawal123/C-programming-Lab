#include <stdio.h>
#include <math.h>

int main()
{
  int n,last,first ,digit;
  printf("Enter a number");
  scanf("%d",&n);
  last=n%10;
  digit=(int)log10(n);
  first=(int)(n/pow (10,digit));
  printf("the first no is %d and last %d",first,last);

    return 0;
}
