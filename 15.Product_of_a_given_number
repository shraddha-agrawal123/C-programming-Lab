#include <stdio.h>
#include <math.h>

int main()
{
  int n,last,product=1;
  printf("Enter a number");
  scanf("%d",&n);
  while(n>0)
  {
      last=n%10;
      product=product*last;
      n=n/10;
  }
  printf(" Product of number is %d",product);
 
    return 0;
}
