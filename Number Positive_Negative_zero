#include <stdio.h>

int
main ()
{
  int a;
  scanf ("%d", &a);
  if (a >0)
    {
      printf ("%d is Positive", a);
    }
  else if (a==0)
    {
      printf (" Number is equal to %d ", a);
    }
    else
    {
        printf("%d is negative",a);
    }
  return 0;
}
